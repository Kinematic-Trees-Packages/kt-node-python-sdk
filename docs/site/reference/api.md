# Python API reference

The reference below is generated from public source and docstrings. Private `ctypes` bindings in `ktnode.abi` are implementation detail.

::: ktnode
    options:
      members: true

## Vision helper

Install optional dependencies with `pip install 'kt-node-python-sdk[vision]'`.

::: ktnode.vision
    options:
      members:
        - make_rgb_image
        - encode_image_sample
        - decode_image_sample_summary
